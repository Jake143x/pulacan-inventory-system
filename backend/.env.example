# Database (required)
# For Supabase / PostgreSQL:
DATABASE_URL="postgresql://postgres.[project-ref]:YOUR_PASSWORD@aws-0-xx.pooler.supabase.com:6543/postgres?pgbouncer=true&sslmode=require"
# For local SQLite (switch prisma/schema.prisma provider to "sqlite" first):
# DATABASE_URL="file:./dev.db"

# JWT secret for auth (change in production)
JWT_SECRET="your-secret-key-change-in-production"

# Optional
# PORT=3001
# LOW_STOCK_THRESHOLD_DEFAULT=10
