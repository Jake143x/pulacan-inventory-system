# Point directly to backend so the app works as long as the backend is running (e.g. npm run dev from project root).
# For production build, set to your backend URL e.g. VITE_API_URL=https://api.example.com/api
VITE_API_URL=http://localhost:3001/api
